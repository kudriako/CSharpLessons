using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CSharpLessons.Fractions.Tests
{
    // http://idms0/confluence/pages/viewpage.action?pageId=28901379
    [TestClass]
    public class FractionCommonTests
    {
        /*[TestMethod]
        public void Fraction_ToString_ShouldDisplayNumeratorAndDenomimnator()
        {
            // Arrange
            int numerator = 5;
            int denominator = 7;
            string str = "5 / 7";

            // Act
            var fraction = new Fraction(numerator, denominator);

            // Assert
            Assert.AreEqual(str, fraction.ToString());
        }*/
    }
}
